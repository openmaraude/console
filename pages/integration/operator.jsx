import Typography from '@material-ui/core/Typography';

import { Layout } from './index';

export default function IntegrationOperatorPage() {
  return (
    <Layout>
      <Typography variant="h4">Simulation d'un opérateur</Typography>
    </Layout>
  );
}
